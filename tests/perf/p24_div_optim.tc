// p24_div_optim: 循环 + 大量除/模常量（应该转换为移位/与）
int main() {
  int s = 0;
  int i = 1;
  while (i < 50000) {
    s = s + i / 2 + i / 4 + i / 8;
    s = s - (i - (i / 2) * 2);
    s = s - (i - (i / 4) * 4);
    s = s + (i - (i / 16) * 16);
    i = i + 1;
  }
  return s % 256;
}
