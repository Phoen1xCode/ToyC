// Dead function with side effects (StoreGlobal) — must NOT be removed if called
int g = 0;
int sideEffect(int x) {
  g = g + x;
  return g;
}
int main() {
  int i = 0;
  while (i < 10) {
    sideEffect(i);
    i = i + 1;
  }
  return g % 256;
}
