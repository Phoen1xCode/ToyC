// p15_ackermann: 极深递归压栈
int ack(int m, int n) {
  if (m == 0) return n + 1;
  if (n == 0) return ack(m - 1, 1);
  return ack(m - 1, ack(m, n - 1));
}
int main() {
  // ack(3,5) = 253
  int r = ack(3, 5);
  return r % 256;
}
